<Window x:Class="CsvViewer.BranchCompareWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="分支比较"
        Width="1180"
        Height="720"
        MinWidth="860"
        MinHeight="520"
        WindowState="Maximized"
        WindowStyle="None"
        ResizeMode="CanResize"
        AllowsTransparency="True"
        Background="Transparent"
        WindowStartupLocation="CenterOwner"
        MouseLeftButtonDown="Window_MouseLeftButtonDown">
    <Border BorderBrush="{DynamicResource AppBorderBrush}"
            BorderThickness="1"
            CornerRadius="18"
            Padding="18"
            Background="{DynamicResource SurfaceBrush}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <DockPanel Grid.Row="0" LastChildFill="True">
                <Button DockPanel.Dock="Right"
                        Content="关闭"
                        Width="78"
                        IsCancel="True"
                        Click="Close_Click" />
                <TextBlock Text="分支比较"
                           FontSize="20"
                           FontWeight="SemiBold"
                           Foreground="{DynamicResource TextPrimaryBrush}" />
            </DockPanel>

            <DockPanel Grid.Row="1" Margin="0,8,0,12" LastChildFill="True">
                <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
                    <Button x:Name="PreviousDifferenceButton"
                            Content="上一处差异"
                            Width="92"
                            Margin="8,0,0,0"
                            Click="PreviousDifference_Click" />
                    <Button x:Name="NextDifferenceButton"
                            Content="下一处差异"
                            Width="92"
                            Margin="8,0,0,0"
                            Click="NextDifference_Click" />
                    <ToggleButton Content="仅显示差异行/列"
                                  Width="128"
                                  Margin="8,0,0,0"
                                  Checked="ShowOnlyDifferences_Changed"
                                  Unchecked="ShowOnlyDifferences_Changed" />
                </StackPanel>
                <TextBlock Text="{Binding SummaryText}"
                           Foreground="{DynamicResource TextSecondaryBrush}"
                           VerticalAlignment="Center" />
            </DockPanel>

            <Grid Grid.Row="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="1" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <Grid Grid.Column="0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <TextBlock Text="{Binding LeftTitle}" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimaryBrush}" Margin="0,0,0,8" />
                    <DataGrid x:Name="LeftDataGrid"
                              Grid.Row="1"
                              AutoGenerateColumns="True"
                              AutoGeneratingColumn="DataGrid_AutoGeneratingColumn"
                              LoadingRow="DataGrid_LoadingRow"
                              ScrollViewer.ScrollChanged="LeftDataGrid_ScrollChanged"
                              SelectedCellsChanged="LeftDataGrid_SelectedCellsChanged"
                              PreviewMouseWheel="DataGrid_PreviewMouseWheel"
                              IsReadOnly="True"
                              CanUserAddRows="False"
                              CanUserDeleteRows="False"
                              CanUserSortColumns="False"
                              SelectionMode="Single"
                              SelectionUnit="Cell"
                              EnableRowVirtualization="True"
                              EnableColumnVirtualization="True"
                              VirtualizingPanel.IsVirtualizing="True"
                              VirtualizingPanel.VirtualizationMode="Recycling"
                              GridLinesVisibility="All"
                              RowHeaderWidth="56" />
                </Grid>

                <Border Grid.Column="1" Background="Black" />

                <Grid Grid.Column="2" Margin="12,0,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <TextBlock Text="{Binding RightTitle}" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimaryBrush}" Margin="0,0,0,8" />
                    <DataGrid x:Name="RightDataGrid"
                              Grid.Row="1"
                              AutoGenerateColumns="True"
                              AutoGeneratingColumn="DataGrid_AutoGeneratingColumn"
                              LoadingRow="DataGrid_LoadingRow"
                              ScrollViewer.ScrollChanged="RightDataGrid_ScrollChanged"
                              SelectedCellsChanged="RightDataGrid_SelectedCellsChanged"
                              PreviewMouseWheel="DataGrid_PreviewMouseWheel"
                              IsReadOnly="True"
                              CanUserAddRows="False"
                              CanUserDeleteRows="False"
                              CanUserSortColumns="False"
                              SelectionMode="Single"
                              SelectionUnit="Cell"
                              EnableRowVirtualization="True"
                              EnableColumnVirtualization="True"
                              VirtualizingPanel.IsVirtualizing="True"
                              VirtualizingPanel.VirtualizationMode="Recycling"
                              GridLinesVisibility="All"
                              RowHeaderWidth="56" />
                </Grid>
            </Grid>
        </Grid>
    </Border>
</Window>
